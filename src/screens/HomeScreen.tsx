import React from "react"
import { Text } from "react-native"
import lang from "../language/lang"

const HomeScreen = () => {
    return(
        <Text>{lang.en.appTitle}</Text>
    )
}

export default HomeScreen;